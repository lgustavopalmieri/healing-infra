# Copy this file to shared.tfvars and fill in the values.
# Run this ONCE before the shared environment.

project_name = "myapp"
environment  = "shared"
aws_region   = "us-east-1"

tags = {
  Team = "platform"
}
