# Copy this file to production.tfvars and fill in the values.
# Run this ONCE before the production environment.

project_name = "myapp"
environment  = "production"
aws_region   = "us-east-1"

tags = {
  Team = "platform"
}
