# Copy this file to staging.tfvars and fill in the values.
# Run this ONCE before the staging environment.

project_name = "myapp"
environment  = "staging"
aws_region   = "us-east-1"

tags = {
  Team = "platform"
}

force_destroy = true
