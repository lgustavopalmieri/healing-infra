# Copy this file to release.tfvars and fill in the values.
# Run this ONCE before the release environment.

project_name = "myapp"
environment  = "release"
aws_region   = "us-east-1"

tags = {
  Team = "platform"
}
